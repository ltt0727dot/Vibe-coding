# 开发日志 — 2026-05-22

## 今日完成

### 1. UI 样式优化
- 所有按钮改为 `RoundedButton`（Canvas 自绘圆角矩形）
- 主色调统一为 #1A365D（深蓝色系）
- 保持极简风格

### 2. 图表功能优化
- 颜色调色板：用户可为每种细胞类型单独选择颜色（colorchooser）
- Y 轴默认 "% of Cytolysis"，X 轴默认 "E:T"
- 图表标题、X/Y 轴标题均可编辑（Entry + 刷新按钮）

### 3. CSV 导出 → XLSX 多 Sheet 导出
- 参考 1.xlsx 格式，导出为 `{输入文件名}_results.xlsx`
- Sheet 1 "详细数据"：每孔一行，同 1.xlsx 格式
- Sheet 2 "% of Cytolysis"：Prism 兼容宽格式，每个细胞类型的个体杀伤值按 E:T 排列
- Sheet 3 "Two-way ANOVA"：ANOVA 结果表 + 事后检验表
- E:T 列强制文本格式（number_format="@"）避免 1:1 变成 0.04

### 4. 显著性计算
- 安装 scipy + statsmodels，Two-way ANOVA 使用 statsmodels ols
- 事后检验：每个 E:T 水平下 t 检验
- 显著性星标 (* p<0.05, ** p<0.01, *** p<0.001) 标注在对比图上
- ANOVA 结果完整写入 xlsx Sheet 3

## 文件变更
- gui.py — 重写（RoundedButton + 新配色 + 颜色选择 + 坐标轴编辑 + ANOVA 集成）
- charts.py — 重写（自定义颜色/轴标题/默认标签/显著性星标）
- export.py — 重写（xlsx 三 sheet 替代 CSV）
- analysis.py — 新增 two_way_anova()
- label_panel.py — 配色更新 #2563EB → #1A365D
- build.spec — 新增 scipy/statsmodels/openpyxl 依赖
- requirements.txt — 新增 scipy, statsmodels, openpyxl

## 测试
- 12 个既有测试全部通过
- ANOVA + xlsx 导出集成测试通过
- 打包：CART_Killing_Analyzer.exe (60MB)

## 项目状态
✅ 全部 4 项修改完成，exe 已就绪
